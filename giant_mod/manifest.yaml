---

id: CMAGiantMod                      # The mod's id. This should not change over the lifetime of the mod.
name: "CMA's Giant mod"               # The mod's name. This is looked up in the language string yaml configuration.
author: "CMAvelis"
description: "CMAvelis attempting to balance giants" # The mod's description. This is looked up in the language string yaml configuration.


...
